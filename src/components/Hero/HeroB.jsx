export default function HeroB({ title }) {
	return (
		<section className="py-16 text-center bg-gray-100 dark:bg-gray-800">
			<h1 className="text-4xl font-bold text-blue-600">{title}</h1>
		</section>
	);
}
